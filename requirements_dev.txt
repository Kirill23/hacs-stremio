# Development Dependencies for Stremio HACS Integration
#
# Versioning policy: avoid version floors on packages that HA or
# pytest-homeassistant-custom-component pin transitively. pip's resolver
# raises ResolutionImpossible whenever our floor exceeds their pin, even
# when the older version works fine for our tests. Let the upstream
# pins win unless we have a specific feature requirement.

# Home Assistant Core (for testing)
homeassistant>=2025.1.0

# Testing
# pytest-homeassistant-custom-component controls pytest, pytest-cov,
# pytest-asyncio, pytest-aiohttp, pytest-timeout etc. — DO NOT add
# version floors here that will conflict with its pins.
pytest-homeassistant-custom-component
pytest-cov

# Async Support
# aiohttp is pinned by HA itself (HA 2025.1.x → aiohttp==3.11.11).
aiohttp
aioresponses

# Code Quality
black
flake8
pylint
mypy
isort
ruff

# Type Stubs
types-requests

# Pre-commit
pre-commit

# Build & Package
build
twine

# Documentation
mkdocs
mkdocs-material

# Debugging
ipython
ipdb
