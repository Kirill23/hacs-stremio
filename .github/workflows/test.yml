name: Test

on:
  workflow_dispatch:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # Home Assistant 2025.x requires Python 3.13+; dropping 3.12.
        # pytest-homeassistant-custom-component>=0.13.206 also requires 3.13.
        python-version: ["3.13"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements_dev.txt
          pip install pytest pytest-asyncio pytest-cov

      - name: Run linters
        run: |
          pip install black flake8 pylint mypy
          black --check custom_components/stremio
          flake8 custom_components/stremio --max-line-length=120
          # pylint custom_components/stremio --disable=C,R

      - name: Run tests
        run: |
          pytest tests/ -v --cov=custom_components/stremio --cov-report=xml

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          fail_ci_if_error: false

  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: HACS Validation
        uses: hacs/action@main
        with:
          category: integration

      - name: Hassfest Validation
        uses: home-assistant/actions/hassfest@master
